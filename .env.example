# Copy to .env in the repository root. Never commit .env.
# Local tools load this file; shell environment variables take precedence.
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-2.5-flare
OPENAI_IMAGE_QUALITY=medium

# Desktop: APP_MODE=demo or birdnet. Paths are relative to the repository.
APP_MODE=demo
APP_PORT=8000
DEMO_FIXTURE=demo/species.json

# Required when APP_MODE=birdnet
BIRDNET_JSON_PATH=